pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo bouwfase'
            }
        }

        stage('Test') {
            steps {
                sh 'echo testfase'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo deployfase'
            }
        }
    }
}